rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Qualquer utilizador autenticado pode ler e escrever em qualquer documento.
    // Isto restaura o comportamento original da app.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
